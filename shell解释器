SHELL解释器

bash优先级
bash用来解释命令。

shell语法：
alias  给操作起别名
unalias  取消别名
a=1;unset a;可以取消给a的赋值，同理适用于函数
hash命令用于缓存命令的路径，内置的命令例如cd没有路径，ls有
定义函数名  function a(){}
read -p a；echo $a



元字符：键盘上的特殊符号，被shell所解释。
*表示所有
～表示家目录
！表示取反
-表示范围，例如a-z，0-9
$?表示上一条命令是否执行成功，0表示成功
#表示注释
$表示取值
%取余
^表示取反
&表示后台运行
&&表示and
||表示or
[a-z]表示所有的大小写
\转义字符
‘’硬引用
“”软引用
；分隔命令
：执行结果永远为真，用来做循环
？表示只匹配一个字符，与*相对应，eg  ls ???.txt  寻找三个字符命名的文件
>覆盖、大于
>>追加

